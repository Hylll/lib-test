import { defineComponent, ref } from 'vue-demi';
export default defineComponent({
    name: 'GButton',
    props: {},
    setup() {
        const foo = ref('foo');
        return {
            foo,
        };
    },
});
//# sourceMappingURL=GButton.vue.js.map